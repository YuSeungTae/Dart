void main() {
  var name = "pizza"; // 방법 1: var키워드 사용
  String name2 = "chicken"; // 방법 2: 명시적 타입 사용
  name = "chicken ";

  //   함수나 메소드 내부에 `지역변수`를 지정할 때는 var를 사용하고
  // class에서 `변수`나 `프로퍼티`를 선언할 때는 명시적 타입을 사용한다.
}